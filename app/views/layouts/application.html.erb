<!DOCTYPE html>
<html lang="en">
  <head>
    <link href='http://fonts.googleapis.com/css?family=Alegreya+SC|Nothing+You+Could+Do' rel='stylesheet' type='text/css'>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Grocery Glyder" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <nav class="top-bar" data-topbar>
        <ul class="title-area">
          <li id="title">
            <h1>Grocery Glyder</h1>
          </li>
        </ul>

        <section class="top-bar-section">
          <!-- Right Nav Section -->
          <ul class="right">
            <%= yield :navr %>
            <li class="userinfo">
              <% if current_user %>
                <% link_to current_user.email, user_path(current_user)%>
                <%= link_to "Log out", logout_path %>
              <% else %>
                <%= link_to "Log in", login_path %> or <%= link_to "Sign up", new_user_path %>
              <% end %>
            </li>
          </ul>

          <ul class="left">
            <p><%= yield :navl %></p>
          </ul>

        </section>
      </nav>
  </header>
      <% if flash[:notice] %>
        <p id="notice"><%= flash[:notice] %></p>
      <% end %>

      <% if flash[:alert] %>
        <p id="alert"><%= flash[:alert] %></p>
      <% end %>

      <%= yield %>

      <%= javascript_include_tag "application" %>
      <script type="text/javascript">$(function(){ $(document).foundation(); });</script>

  </body>
</html>